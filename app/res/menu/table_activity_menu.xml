<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
            android:id="@+id/add_row_button"
            android:icon="@drawable/ic_menu_add"
            android:showAsAction="always"
            android:title="@string/create_record">
    </item>

    <item
            android:id="@+id/tariff_button"
            android:icon="@drawable/ic_coin"
            android:showAsAction="always"
            android:title="@string/add_tariff">
    </item>

    <item
            android:id="@+id/show_graph_button"
            android:icon="@drawable/ic_graph"
            android:showAsAction="always"
            android:title="@string/show_graph">
    </item>

</menu>